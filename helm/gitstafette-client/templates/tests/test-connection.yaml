apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitstafette-client.fullname" . }}-test-connection"
  labels:
    {{- include "gitstafette-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitstafette-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
